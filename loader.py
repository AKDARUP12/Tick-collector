"""RETIRED scaffold stub — use run_collect.py (live / --dry-run) instead.

The old CSV-era demo that lived here was removed: it hardcoded credentials and
wrote ever-growing CSVs, both of which the current pipeline replaced (Parquet
tick lake, secrets only from .env). See README.md and docs/PLAN.md.
"""
raise SystemExit("loader.py retired — use: python run_collect.py [--dry-run]")
